var todos = [];
var input = prompt("what would you like to do?");
if(input=== "list"){
    console.log(todos);
} eles if (input === "new"){
var newTodo = prompt("Enter new todo");
todos.push(newTodo);
}